name: CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  scan_ruby:
    runs-on:
      - self-hosted
      - linux
      - home

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure rbenv
        run: |
          echo "RBENV_ROOT=$HOME/.rbenv" >> "$GITHUB_ENV"
          echo "$HOME/.rbenv/bin" >> "$GITHUB_PATH"
          echo "$HOME/.rbenv/shims" >> "$GITHUB_PATH"
          export RBENV_ROOT="$HOME/.rbenv"
          export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"
          rbenv global 4.0.0
          rbenv rehash
          ruby -v

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Scan for common Rails security vulnerabilities using static analysis
        run: bin/brakeman --no-pager
      
      - name: Scan for known security vulnerabilities in gems used
        run: bin/bundler-audit
      
  scan_js:
    runs-on:
      - self-hosted
      - linux
      - home

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure rbenv
        run: |
          echo "RBENV_ROOT=$HOME/.rbenv" >> "$GITHUB_ENV"
          echo "$HOME/.rbenv/bin" >> "$GITHUB_PATH"
          echo "$HOME/.rbenv/shims" >> "$GITHUB_PATH"
          export RBENV_ROOT="$HOME/.rbenv"
          export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"
          rbenv global 4.0.0
          rbenv rehash
          ruby -v

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Scan for security vulnerabilities in JavaScript dependencies
        run: bin/importmap audit

  lint:
    runs-on:
      - self-hosted
      - linux
      - home
    env:
      RUBOCOP_CACHE_ROOT: tmp/rubocop
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure rbenv
        run: |
          echo "RBENV_ROOT=$HOME/.rbenv" >> "$GITHUB_ENV"
          echo "$HOME/.rbenv/bin" >> "$GITHUB_PATH"
          echo "$HOME/.rbenv/shims" >> "$GITHUB_PATH"
          export RBENV_ROOT="$HOME/.rbenv"
          export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"
          rbenv global 4.0.0
          rbenv rehash
          ruby -v

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Prepare RuboCop cache
        uses: actions/cache@v4
        env:
          DEPENDENCIES_HASH: ${{ hashFiles('.ruby-version', '**/.rubocop.yml', '**/.rubocop_todo.yml', 'Gemfile.lock') }}
        with:
          path: ${{ env.RUBOCOP_CACHE_ROOT }}
          key: rubocop-${{ runner.os }}-${{ env.DEPENDENCIES_HASH }}-${{ github.ref_name == github.event.repository.default_branch && github.run_id || 'default' }}
          restore-keys: |
            rubocop-${{ runner.os }}-${{ env.DEPENDENCIES_HASH }}-

      - name: Lint code for consistent style
        run: bin/rubocop -f github

  test:
    runs-on:
      - self-hosted
      - linux
      - home
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: today_we_ate_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 65432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d today_we_ate_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 6
    env:
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:65432/today_we_ate_test

    # services:
    #  redis:
    #    image: valkey/valkey:8
    #    ports:
    #      - 6379:6379
    #    options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure rbenv
        run: |
          echo "RBENV_ROOT=$HOME/.rbenv" >> "$GITHUB_ENV"
          echo "$HOME/.rbenv/bin" >> "$GITHUB_PATH"
          echo "$HOME/.rbenv/shims" >> "$GITHUB_PATH"
          export RBENV_ROOT="$HOME/.rbenv"
          export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"
          rbenv global 4.0.0
          rbenv rehash
          ruby -v

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Run tests
        env:
          RAILS_ENV: test
          # RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY }}
          # REDIS_URL: redis://localhost:6379/0
        run: bin/rails db:test:prepare test

  system-test:
    runs-on:
      - self-hosted
      - linux
      - home
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: today_we_ate_system_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 65433:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d today_we_ate_system_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 6
    env:
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:65433/today_we_ate_system_test

    # services:
    #  redis:
    #    image: valkey/valkey:8
    #    ports:
    #      - 6379:6379
    #    options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure rbenv
        run: |
          echo "RBENV_ROOT=$HOME/.rbenv" >> "$GITHUB_ENV"
          echo "$HOME/.rbenv/bin" >> "$GITHUB_PATH"
          echo "$HOME/.rbenv/shims" >> "$GITHUB_PATH"
          export RBENV_ROOT="$HOME/.rbenv"
          export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"
          rbenv global 4.0.0
          rbenv rehash
          ruby -v

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Cache bundle
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: bundle-${{ runner.os }}-${{ hashFiles('**/Gemfile.lock') }}
          restore-keys: |
            bundle-${{ runner.os }}-

      - name: Install gems
        run: |
          bundle config set path vendor/bundle
          bundle install --jobs 4 --retry 3

      - name: Run System Tests
        env:
          RAILS_ENV: test
          # RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY }}
          # REDIS_URL: redis://localhost:6379/0
        run: bin/rails db:test:prepare test:system

      - name: Keep screenshots from failed system tests
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: screenshots
          path: ${{ github.workspace }}/tmp/screenshots
          if-no-files-found: ignore
